# 数据库连接字符串
# 格式：postgresql://用户名:密码@主机:端口/数据库名?schema=public
DATABASE_URL="postgresql://user:password@localhost:5432/pawll?schema=public"

# NextAuth.js 配置
# 网站URL（开发环境：http://localhost:3000，生产环境：https://pawllpet.com）
NEXTAUTH_URL="http://localhost:3000"
# NextAuth.js 加密密钥（用于加密JWT和会话token）
# 生成方式：openssl rand -base64 32
NEXTAUTH_SECRET="your-nextauth-secret-key-here"

# Google OAuth 2.0 配置
# 在 Google Cloud Console 创建 OAuth 2.0 客户端ID和密钥
# https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# 邮件服务配置（Resend）
# 注册账号：https://resend.com
# 获取API密钥：https://resend.com/api-keys
RESEND_API_KEY="your-resend-api-key"
# 发件人邮箱（需要在Resend中验证）
EMAIL_FROM="noreply@pawllpet.com"
EMAIL_FROM_NAME="PawLL"

# 可选：如果使用SMTP（Nodemailer）而不是Resend
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_USER="your-email@gmail.com"
# SMTP_PASSWORD="your-app-password"

# 应用配置
# 邮箱验证token过期时间（小时）
EMAIL_VERIFICATION_EXPIRES_HOURS=24

